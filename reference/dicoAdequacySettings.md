# Correspondence between arguments of `updateAdequacySettings` and actual Antares parameters.

Correspondence between arguments of `updateAdequacySettings` and actual
Antares parameters.

## Usage

``` r
dicoAdequacySettings(arg)
```

## Arguments

- arg:

  An argument from function `updateAdequacySettings`.

## Value

The corresponding Antares general parameter.
