# Clean up output based on geographic trimming

Clean up output based on geographic trimming

## Usage

``` r
cleanUpOutput(areas = NULL, opts = simOptions())
```

## Arguments

- areas:

  Character. vector of areas (folders). Links will also be cleaned based
  on getLinks() results

- opts:

  List. simulation options
