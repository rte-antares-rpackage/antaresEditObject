# function that ensures the transition to 9

basic tests on the version's writing format

## Usage

``` r
.is_version_9(version)
```

## Arguments

- version:

  `character` (eg, "8.2.0" or "9.0")

## Value

`logical` if version \>=9
